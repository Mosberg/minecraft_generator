rootProject.name = "minecraft_generator"

// Optional: improves dependency resolution consistency and keeps repos centralized.
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
    }
}
